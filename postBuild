#!/bin/sh
set -x

mamba run -n base bash -c "echo \${CONDA_PREFIX}"

mamba run -n notebook bash -c "echo \${CONDA_PREFIX}"

mamba run -n notebook bash -c "echo \${CXXFLAGS}"

echo $CONDA_PREFIX

echo $MAMBA_ROOT_PREFIX